#include <stdio.h>

int main() {
    int opcion;

    do {
        printf("\n=== MENU SIMPLE ===\n");
        printf("1. Decir hola\n");
        printf("2. Decir adiós\n");
        printf("3. Salir\n");
        printf("Elige una opcion: ");
        scanf("%d", &opcion);

        switch (opcion) {
            case 1:
                printf("👋 ¡Hola!\n");
                break;
            case 2:
                printf("👋 ¡Adiós!\n");
                break;
            case 3:
                printf("Saliendo...\n");
                break;
            default:
                printf("Opción no válida.\n");
        }

    } while (opcion != 3);

    return 0;
}
